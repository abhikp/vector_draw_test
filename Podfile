platform :ios, '10.0'

target 'vector_draw_test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for vector_draw_test
  pod "Macaw", '0.9.1'
end
